{
  "buildCommand": "pnpm --filter @workspace/jupeb-prep run build && mkdir -p dist && cp -r artifacts/jupeb-prep/dist/public/. dist/",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
